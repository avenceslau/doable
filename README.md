# doable

A tiny starter package currently exporting an `identity` helper.

## Install

```bash
npm install doable
```

## Usage

```js
const { identity } = require("doable");

identity("hello");
// => "hello"
```
